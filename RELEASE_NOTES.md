## Bug Fixes

- Plan-to-execute handoff now triggers only when a plan is actually approved, not on every exit from plan mode.
- Stabilized cross-platform UI tests so the suite runs reliably on Linux and CI.
